{
  "name": "starry-ledger",
  "version": "0.2.0",
  "private": true,
  "description": "Starry Ledger rewritten as a Next.js frontend and Cloudflare Workers + D1 backend.",
  "workspaces": [
    "apps/web",
    "apps/api"
  ],
  "scripts": {
    "dev:web": "npm run dev -w apps/web",
    "dev:api": "npm run dev -w apps/api",
    "build": "npm run build -w apps/web && npm run build -w apps/api",
    "typecheck": "npm run typecheck -w apps/web && npm run typecheck -w apps/api",
    "db:migrate:local": "npm run db:migrate:local -w apps/api",
    "db:migrate:remote": "npm run db:migrate:remote -w apps/api",
    "deploy:web": "npm run deploy -w apps/web",
    "deploy:api": "npm run deploy -w apps/api"
  },
  "engines": {
    "node": ">=22"
  }
}
